<%= render 'public/travel_memories/errors', obj: travel_memory_comment %>
<%= form_with model: [travel_memory, travel_memory_comment] do |f| %>
  <%= f.text_area :comment, rows:'5', placeholder: 'コメントをここに', class: 'w-100' %>
  <%= f.submit '送信する' %>
<% end %>