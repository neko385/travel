<% if customer_signed_in? %>
  <div ckass="search_form">
    <%= form_with url: search_path, method: :get do |f| %>
      <%= f.text_field :keyword %>
      <%= f.select :range, options_for_select([["ユーザー", "Customer"], ["投稿", "TravelMemory"]]) %>
      <%= f.submit "検索", class: "btn btn-primary" %>
    <% end %>
  </div>
<% end %>